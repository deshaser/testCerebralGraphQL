export default theme => ({
  title: {
    margin: `${theme.spacing.unit * 2}px auto`,
    textAlign: 'center',
  },
  progress: {
    display: 'block',
    margin: `${theme.spacing.unit * 2}px auto`,
  },
  snackbar: {
    margin: theme.spacing.unit,
  },
})
